name: ShellCheck

on:
  push:
  pull_request:

jobs:
  shellcheck:
    permissions:
      contents: read
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: ShellCheck
        uses: ludeeus/action-shellcheck@master
