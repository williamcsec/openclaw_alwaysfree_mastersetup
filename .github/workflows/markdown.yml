name: Markdown Check

on:
  push:
  pull_request:

jobs:
  markdown:
    permissions:
      contents: read
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Markdown Lint
        uses: avto-dev/markdown-lint@1.1.0
